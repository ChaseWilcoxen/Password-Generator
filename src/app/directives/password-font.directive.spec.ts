import { PasswordFontDirective } from './password-font.directive';

describe('PasswordFontDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordFontDirective();
    expect(directive).toBeTruthy();
  });
});
